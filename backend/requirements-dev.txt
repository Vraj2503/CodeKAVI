# ── Dev dependencies (install with: pip install -r requirements-dev.txt) ──
# These are NOT needed in production — only for development and CI.

# Testing
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27                # async test client for FastAPI
coverage[toml]>=7.0        # coverage with pyproject.toml config

# Linting & Formatting
ruff>=0.8

# Type checking
mypy>=1.13

# Pre-commit hooks
pre-commit>=4.0
