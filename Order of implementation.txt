Stage A — Do today · ~1 day

Source: review_implementation.md Sprint 0 + IMPL-4

┌────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  Item  │                                                    What                                                    │
├────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-1 │ .dockerignore for backend + frontend, then rotate every key (GROQ, GEMINI, CLOUDFLARE, ZILLIZ,             │
│        │ SUPABASE_SERVICE_KEY, SUPABASE_JWT_SECRET)                                                                 │
├────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-2 │ Non-root container — useradd + USER appuser in the Dockerfile runner stage                                 │
├────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-4 │ vectorstore.py:352 → sort(key=..., reverse=True) and fix the comment                                       │
└────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

IMPL-4 is pulled forward out of Sprint 1 because it's one line, and until it lands your chat returns the least-relevant chunks — so you can't judge how much chat can carry.

Stage B — Foundation · ~1.5 days

Source: review_implementation.md Sprint 1 = plan Steps 0.2 and 1

┌────────┬────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  Item  │                                           Why before the feature                                           │
├────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-5 │ assert_repo_owner. The graph endpoint takes a repo_id; adding authz after means retrofitting three         │
│        │ endpoints instead of one                                                                                   │
├────────┼────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-3 │ Test suite + CI collected-count guard. The spec's determinism guarantee is untestable without it           │
└────────┴────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

Stage C — Graph phase 1 · ~10 days

Source: docs/superpowers/plans/2026-07-25-graph-phase1-plan.md, steps 2–10

Steps 0 and 1 of that plan are Stage B above — don't run them twice.

2 → containers · 3 → flags · 4 → assembly/edges/portals · 5 → endpoint · 6 → frontend scaffolding · 7 → ELK worker · 8 → components · 9 → page · 10 → verification

Backend (2–5) is ~3.5 days and independently valuable.

Stage D — Before real users · ~4 days

Source: review_implementation.md Sprints 2–3, re-tiered

┌─────────┬───────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│  Item   │                                                   What                                                    │
├─────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-12 │ Zilliz missing-field drift drops the entire shared collection — every user's vectors. Data loss;          │
│         │ mis-filed as Sprint 3 polish                                                                              │
├─────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-7  │ Bounded caches (OOM)                                                                                      │
├─────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-10 │ Token quota on by default (cost-DoS)                                                                      │
├─────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-8  │ Rate limiting fail-closed                                                                                 │
├─────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-11 │ Clone caps during clone                                                                                   │
├─────────┼───────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ IMPL-9  │ X-Forwarded-For trust                                                                                     │
└─────────┴───────────────────────────────────────────────────────────────────────────────────────────────────────────┘

Can run in parallel with Stage C review — it gates users, not development.

Stage E — Phase 2, the tour · ~5 days

Source: docs/superpowers/specs/2026-07-25-graph-and-tour-design.md §8

tour_generator.py already has Kahn's-ordering + importance-sort fallback (built for the /explain
degraded path) — E1/E2 adapt it, they don't start from scratch.

┌──────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Item │                                                       What                                                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E1   │ Learn-mode ordering — layer-tier bottom-up (config/types → core → routes), reuse existing Kahn's pass        │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E2   │ Recall-mode ordering — importance_score desc, then flagged files; reuse the cyclic-fallback sort as the seed │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E3   │ Question generation — flag→template table (orphan/in_cycle/god_file/hub/entry_point). Zero LLM, pure function│
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E4   │ IMPL-19 prompt-injection framing — gate BEFORE any cached LLM tour prose ships; dedup makes cached prose      │
│      │ cross-user, higher injection blast radius than chat                                                         │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E5   │ Tour endpoint — GET /api/graph/{repo_id}/tour?mode=learn|recall, assembles E1–E4, routed through             │
│      │ assert_repo_owner (IMPL-5)                                                                                   │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E6   │ Frontend stepper panel — renders ordered steps, title/facts/questions, next/prev                            │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E7   │ Camera trap (TourFitView) — expand collapsed container, poll rAF for measured dims, fitView; ~4s ceiling     │
│      │ falls back to fitting the layer                                                                             │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E8   │ Progress persistence — localStorage `codekavi-tour-progress-{repoId}`, steps seen + questions marked         │
│      │ "can answer"                                                                                                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ E9   │ Verification — both modes produce same steps/facts in different order+register; determinism test; camera-   │
│      │ trap fallback exercised                                                                                     │
└──────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

E1–E3 are backend-only and independently valuable (same split as Stage C's backend/frontend divide).
E4 gates E5 only if LLM-generated prose ships with phase 1 — the questions themselves (E3) are
deterministic and can ship without it.

Stage F — Everything else

Source: review_implementation.md Sprint 1/3 leftovers + phase 3

┌──────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Sub  │                                                       Scope                                                │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ F1 ✅ │ DONE — IMPL-6 (json_mode now threaded through GroqProvider._generate_core, providers.py:388),               │
│      │ IMPL-15 (CORS origins via parsed_cors_origins(), main.py:107), IMPL-17 (byte-safe chunk truncation,          │
│      │ indexer.py:204-206, encode before slicing at 65000).                                                         │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ F2 ✅ │ DONE — IMPL-18's four items: vectorstore.search docstring fixed (vectorstore.py:273), CircuitBreaker         │
│      │ .snapshot() now takes the lock (providers.py:94), ensure_repo_loaded's 202 HTTPException (session.py:158)    │
│      │ kept and documented with a comment (zero behavior change), .gitlab-ci.yml removed.                          │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ F3 ✅ │ DONE — IMPL-13: shared `_run_pipeline(...) -> PipelineResult` (routes/analyze.py:82) consumed by both        │
│      │ /analyze and /analyze/stream; mermaid unified as {file_level, module_level} on PipelineResult. Contract      │
│      │ test: test_analyze_pipeline_contract.py asserts identical graph/mermaid for both routes.                     │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ F4 ✅ │ DONE — IMPL-14 (requirements.in/.txt compiled lockfile, pip-audit in CI, LLM SDKs pinned) and IMPL-16        │
│      │ (GET /api/health/deep, rate-limited, pings Redis/Zilliz/Supabase).                                          │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ F5   │ Phase 3: question-driven tours (G1–G3) + diff tours (H1–H5). Spec written:                                   │
│      │ docs/superpowers/specs/2026-07-26-phase3-question-diff-tours-design.md. Not started — see breakdown below.  │
└──────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

F1–F4 are done. F5 is unblocked (spec exists) — none of G1–G3/H1–H5 below are started yet.

F5 breakdown — Source: docs/superpowers/specs/2026-07-26-phase3-question-diff-tours-design.md

Both G and H are new step sources for the existing TourPanel/assemble_tour payload shape — no new
frontend stepper or camera-trap work, per the parent spec's "component doesn't care where steps came
from" decision. G and H don't depend on each other and can run in parallel.

┌──────┬─────────────────────────────────────────────────────────────────────────────────────────────────────────────┐
│ Item │                                                       What                                                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ G1   │ ~0.5d — resolve_question_nodes(graph, search_results) in tour_generator.py: dedupe zilliz_client.search()    │
│      │ hits by file_path, preserve retrieval rank, drop hits whose file_path isn't a graph node id.                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ G2   │ ~0.5d — generate_question_tour(graph, node_ids): tier-order via existing _kahn_order restricted to           │
│      │ node_ids, ties broken by retrieval rank. Feeds assemble_tour a pre-built steps skeleton (no new mode         │
│      │ branch needed).                                                                                              │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ G3   │ ~0.5d — GET /graph/semantic/{repo_id}/tour/question?q=... . Only phase-3 endpoint that costs money           │
│      │ (embeds the query) — reuse chat.py's quota check + a tighter per_minute than the free structural             │
│      │ endpoints; assert_repo_owner via _load_repo.                                                                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ H1   │ ~0.25d — persist last_change_map: attach {path: fp.change_type} from compare_and_classify_repo's             │
│      │ fingerprints to the cached result (routes/analyze.py:409, 665) before cache.set. Rides the existing          │
│      │ cache write, no new storage.                                                                                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ H2   │ ~1d — generate_diff_tour(graph, change_map) in tour_generator.py: STRUCTURAL then COSMETIC files,            │
│      │ importance desc within each group, one step per file. Deleted files (in change_map, absent from graph)      │
│      │ dropped from steps, surfaced as a count instead (see H4).                                                    │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ H3   │ ~0.5d — GET /graph/semantic/{repo_id}/tour/diff. Reads last_change_map off the cached result; 404            │
│      │ (with a distinguishing message) if absent — must not conflate "no prior analysis" with "nothing changed".   │
│      │ Zero LLM calls, free per_minute tier.                                                                        │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ H4   │ ~1d — TourPanel gets a third entry ("What changed" → H3) and a question box (→ G3). Deleted-files banner     │
│      │ renders above the stepper when deleted_count > 0. Only item touching React.                                 │
├──────┼─────────────────────────────────────────────────────────────────────────────────────────────────────────────┤
│ H5   │ ~0.5d — Determinism test for generate_diff_tour over fixture change-maps; endpoint tests (404 on no-prior-   │
│      │ analysis, IDOR denial). Shares test-restoration work with F3 — test_graph_routes.py needs restoring first,   │
│      │ do it once for both.                                                                                         │
└──────┴─────────────────────────────────────────────────────────────────────────────────────────────────────────────┘

G1–G3 ≈ 1.5 days, H1–H5 ≈ 3.25 days, F5 total ≈ 5 days. G1–G3 and H1–H3 ship value with zero frontend
work (hit endpoints directly); H4 is the only React item.

---
The gates, in one line each: Stage A because it's actively harmful. Stage B because the feature inherits its problems otherwise. Stage C is the feature. Stage D before strangers can reach you. E and F after.